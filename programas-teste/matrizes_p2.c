#include <stdio.h>

int calculoSalarial() {

  const float imposto;
  float salario;

  printf("Qual o seu salario(total)? = ");
  scanf("%f", &salario);

  printf("Qual o imposto(em porcentagem)? = ");
  scanf("%f", &imposto);
}

int main() {

}